#include<stdio.h>
#include<string.h>

#define N = 10
#define INC(x) x+1
#define SUB (x,y) x-y
#define SQR(x) ((x)*(x))
#define CUBE(x) (SQR(x)*(x))
#define M1(x,y) x##y
#define M2(x,y) #x #y

int main(){
	
	int a[N];
	int a[=10], i, j, k, m;/*=10*/
  
  	i=j;
	
	i=10 * INC(j);
  	i=10*j+1;
	
	i=SUB(j, k);
  	i=(x,y)x-y(j,k);

  	i=((((j)*(j)))*(((j)*(j))));
  	i= (((j)*(j))*(j));
  	i=jk;/*j k undeclared*/
  	puts("i" "j");

  	i=SQR(j);/*redefine the SQR*/
  	i=((j)*(j));
  
  return 0;
}
